import Graph from "@/components/Graph";
import Prefectures from "@/components/Prefectures";

const Main = () => {
	return (
		<main>
			<Prefectures />
			<Graph />
		</main>
	);
};

export default Main;
